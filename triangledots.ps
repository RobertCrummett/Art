%!PS-Adobe-3.0 EPSF-3.0
%%BoundingBox: 0 0 100 100
/A [40 10] def
/B [90 50] def
/C [20 80] def

/plotdot {
newpath
aload pop moveto 
0 0 rlineto
stroke
} def

/midpoint {
aload pop
3 -1 roll
aload pop
exch
4 -1 roll
add 2 div
3 1 roll
add 2 div
2 array astore
} def

2 4 div setlinewidth

newpath
A aload pop moveto
B aload pop lineto
C aload pop lineto
closepath
stroke

newpath
A aload pop moveto
B C midpoint aload pop lineto
stroke

newpath
B aload pop moveto
A C midpoint aload pop lineto
stroke

newpath
C aload pop moveto
A B midpoint aload pop lineto
stroke

1 setlinecap
3 setlinewidth

A plotdot
B plotdot
C plotdot

A C midpoint plotdot
A B midpoint plotdot
B C midpoint plotdot

A aload pop
B aload pop exch 4 1 roll
C aload pop exch 6 1 roll
add add 3 div
4 1 roll
add add 3 div 
exch
2 array astore plotdot
%%EOF
